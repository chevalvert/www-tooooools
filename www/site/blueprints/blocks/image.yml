name: field.blocks.image.name
icon: image
preview: image
fields:
  image:
    label: field.blocks.image.name
    type: files
    multiple: false
    image:
      cover: true

  # TODO: implement this in php
  caption:
    label: field.blocks.image.caption
    type: writer
    icon: text
    inline: true
